.result-panel {
  background: rgba(255, 255, 255, 0.95);
  border-radius: 20rpx;
  padding: 30rpx;
  margin-bottom: 30rpx;
  border: 2rpx solid #e0e0e0;
}

.result-title {
  font-size: 36rpx;
  font-weight: bold;
  color: #333;
  text-align: center;
  margin-bottom: 10rpx;
}

.result-reason {
  text-align: center;
  color: #666;
  font-size: 28rpx;
  margin-bottom: 20rpx;
}

.role-list {
  display: flex;
  flex-direction: column;
  gap: 10rpx;
}

.role-item {
  display: flex;
  align-items: center;
  justify-content: space-between;
  background: #f7f7f7;
  border-radius: 12rpx;
  padding: 20rpx;
}

.role-number {
  font-weight: bold;
  color: #667eea;
  width: 120rpx;
}

.role-name {
  flex: 1;
  color: #333;
}

.role-badge {
  min-width: 140rpx;
  text-align: center;
  padding: 10rpx 20rpx;
  border-radius: 30rpx;
  font-weight: bold;
  color: white;
  background: #9e9e9e;
}

.role-badge.good {
  background: #4caf50;
}

.role-badge.evil {
  background: #f44336;
}

/* pages/game/game.wxss */
.container {
  min-height: 100vh;
  background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
  padding: 40rpx;
  padding-bottom: 100rpx;
}

.top-row {
  display: flex;
  align-items: center;
  justify-content: space-between;
  gap: 16rpx;
  flex-wrap: nowrap;
  width: 100%;
  margin-bottom: 30rpx;
}

.action-btn {
  flex: 1;
  min-width: 0;
  max-width: none;
  padding: 14rpx 22rpx;
  background: rgba(255, 255, 255, 0.2);
  border-radius: 32rpx;
  color: white;
  font-size: 26rpx;
  text-align: center;
  box-shadow: 0 4rpx 12rpx rgba(0, 0, 0, 0.1);
  line-height: 1.3;
  flex-shrink: 0;
}

.action-btn text {
  display: block;
  white-space: nowrap;
}

.action-btn.danger {
  background: rgba(255, 107, 107, 0.45);
}

.loading {
  display: flex;
  align-items: center;
  justify-content: center;
  min-height: 60vh;
  color: white;
  font-size: 32rpx;
}

.content {
  padding-top: 20rpx;
}

/* 游戏进度 */
.game-progress {
  background: rgba(255, 255, 255, 0.95);
  border-radius: 20rpx;
  padding: 30rpx;
  margin-bottom: 30rpx;
}

.mission-track {
  display: flex;
  justify-content: space-between;
  margin-bottom: 30rpx;
}

.mission-item {
  flex: 1;
  text-align: center;
  padding: 20rpx 10rpx;
  background: #f5f5f5;
  border-radius: 12rpx;
  margin: 0 5rpx;
  position: relative;
  transition: all 0.3s;
  box-shadow: 0 2rpx 8rpx rgba(0, 0, 0, 0.1);
}

.mission-item-hover {
  transform: scale(1.03);
  box-shadow: 0 6rpx 16rpx rgba(102, 126, 234, 0.35);
}

.mission-item.active {
  background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
  color: white;
}

.mission-item.mission-success {
  background: linear-gradient(135deg, #42a5f5 0%, #1976d2 100%);
  color: white;
}

.mission-item.mission-failed {
  background: linear-gradient(135deg, #ef5350 0%, #c62828 100%);
  color: white;
}

.mission-number {
  font-size: 24rpx;
  margin-bottom: 5rpx;
}

.mission-players {
  font-size: 20rpx;
  opacity: 0.8;
}

.mission-protection {
  font-size: 18rpx;
  opacity: 0.7;
  margin-top: 2rpx;
  color: #ff9800;
  font-weight: bold;
}

.mission-result-badge {
  position: absolute;
  top: -10rpx;
  right: -10rpx;
  width: 40rpx;
  height: 40rpx;
  background: white;
  border-radius: 50%;
  display: flex;
  align-items: center;
  justify-content: center;
  font-size: 24rpx;
  font-weight: bold;
  box-shadow: 0 2rpx 8rpx rgba(0, 0, 0, 0.2);
}

.mission-success .mission-result-badge {
  background: #4caf50;
  color: white;
}

.mission-failed .mission-result-badge {
  background: #f44336;
  color: white;
}

.score-display {
  display: flex;
  justify-content: space-around;
  padding-top: 20rpx;
  border-top: 2rpx solid #e5e5e5;
}

.score-item {
  display: flex;
  align-items: center;
  gap: 15rpx;
}

.score-label {
  font-size: 28rpx;
  color: #666;
}

.score-value {
  font-size: 48rpx;
  font-weight: bold;
}

.score-item.good .score-value {
  color: #4caf50;
}

.score-item.evil .score-value {
  color: #f44336;
}

/* 用户模块 */
.users-section {
  background: rgba(255, 255, 255, 0.95);
  border-radius: 20rpx;
  padding: 30rpx;
  margin-bottom: 30rpx;
}

.section-title {
  font-size: 32rpx;
  font-weight: bold;
  color: #333;
  text-align: center;
  margin-bottom: 30rpx;
  padding-bottom: 20rpx;
  border-bottom: 2rpx solid #e5e5e5;
}

.users-grid {
  display: grid;
  grid-template-columns: repeat(3, 1fr);
  gap: 15rpx;
  margin-bottom: 30rpx;
}

.user-card-wrapper {
  position: relative;
}

.user-card {
  background: #f5f5f5;
  border-radius: 12rpx;
  padding: 20rpx 15rpx;
  text-align: center;
  border: 3rpx solid transparent;
  transition: all 0.3s ease;
  position: relative;
  min-height: 140rpx;
  display: flex;
  flex-direction: column;
  justify-content: center;
  align-items: center;
}

.user-card.selectable {
  cursor: pointer;
}

.user-card.selectable:active {
  transform: scale(0.95);
}

.user-card.selected {
  background: linear-gradient(135deg, #e8f5e9 0%, #c8e6c9 100%);
  border-color: #4caf50;
  border-width: 4rpx;
  box-shadow: 0 4rpx 12rpx rgba(76, 175, 80, 0.4);
  transform: scale(1.05);
}

.user-card.is-leader {
  background: linear-gradient(135deg, #fff9e6 0%, #ffe0b2 100%);
  border-color: #ff9800;
  border-width: 3rpx;
}

.user-card.is-leader.selected {
  background: linear-gradient(135deg, #e8f5e9 0%, #c8e6c9 100%);
  border-color: #4caf50;
}

.user-card.nominated {
  border-color: #2196f3;
  border-width: 3rpx;
  background: linear-gradient(135deg, #e3f2fd 0%, #bbdefb 100%);
}

.user-card-header {
  display: flex;
  align-items: center;
  justify-content: center;
  gap: 8rpx;
  margin-bottom: 10rpx;
  width: 100%;
  position: relative;
}

.user-number {
  font-size: 32rpx;
  font-weight: bold;
  color: #667eea;
}

.leader-indicator {
  position: absolute;
  top: -20rpx;
  left: 50%;
  transform: translateX(-50%);
  font-size: 22rpx;
  color: #ff9800;
  font-weight: bold;
  white-space: nowrap;
  background: rgba(255, 255, 255, 0.95);
  padding: 6rpx 16rpx;
  border-radius: 20rpx;
  border: 2rpx solid #ff9800;
  z-index: 10;
  box-shadow: 0 2rpx 8rpx rgba(255, 152, 0, 0.3);
}

.nominated-indicator {
  font-size: 24rpx;
  color: #2196f3;
  font-weight: bold;
  position: absolute;
  top: 5rpx;
  left: 5rpx;
  width: 36rpx;
  height: 36rpx;
  display: flex;
  align-items: center;
  justify-content: center;
  background: rgba(33, 150, 243, 0.2);
  border-radius: 50%;
  border: 2rpx solid #2196f3;
}

.user-name {
  font-size: 24rpx;
  color: #666;
  margin-top: 5rpx;
}

.selected-badge {
  position: absolute;
  top: 5rpx;
  right: 5rpx;
  width: 40rpx;
  height: 40rpx;
  background: #4caf50;
  color: white;
  border-radius: 50%;
  display: flex;
  align-items: center;
  justify-content: center;
  font-size: 24rpx;
  font-weight: bold;
  box-shadow: 0 2rpx 8rpx rgba(76, 175, 80, 0.6);
}

/* 提名阶段操作 */
.nomination-actions {
  margin-top: 20rpx;
}

.selected-count {
  text-align: center;
  font-size: 28rpx;
  color: #666;
  margin-bottom: 20rpx;
}

.waiting-message {
  text-align: center;
  padding: 40rpx 20rpx;
  color: #999;
  font-size: 28rpx;
}

/* 投票阶段操作 */
.vote-actions {
  margin-top: 20rpx;
}

.vote-buttons {
  display: flex;
  gap: 20rpx;
  margin-bottom: 20rpx;
}

.vote-tip {
  text-align: center;
  font-size: 24rpx;
  color: #999;
}

.mission-message {
  text-align: center;
  padding: 40rpx 20rpx;
  color: #666;
  font-size: 28rpx;
}

.warning-banner {
  margin-top: 20rpx;
  padding: 20rpx;
  background: #fff3cd;
  border-left: 4rpx solid #ffc107;
  border-radius: 8rpx;
  color: #856404;
  font-size: 26rpx;
  text-align: center;
}

/* 按钮 */
.btn {
  width: 100%;
  height: 88rpx;
  line-height: 88rpx;
  border-radius: 44rpx;
  font-size: 32rpx;
  border: none;
}

.btn-primary {
  background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
  color: white;
  font-weight: bold;
}

.btn-primary[disabled] {
  background: #cccccc;
  color: #999;
}

.btn-approve {
  flex: 1;
  background: #4caf50;
  color: white;
  font-weight: bold;
  transition: all 0.3s;
}

.btn-approve[disabled] {
  background: #a5d6a7;
  color: #f5f5f5;
}

.btn-approve.vote-selected {
  background: #2e7d32;
  box-shadow: 0 0 0 4rpx rgba(76, 175, 80, 0.5);
  transform: scale(1.05);
}

.btn-reject {
  flex: 1;
  background: #f44336;
  color: white;
  font-weight: bold;
  transition: all 0.3s;
}

.btn-reject[disabled] {
  background: #ef9a9a;
  color: #f5f5f5;
}

.btn-reject.vote-selected {
  background: #c62828;
  box-shadow: 0 0 0 4rpx rgba(244, 67, 54, 0.5);
  transform: scale(1.05);
}

/* 身份弹窗 */
.modal-overlay {
  position: fixed;
  top: 0;
  left: 0;
  right: 0;
  bottom: 0;
  background: rgba(0, 0, 0, 0.7);
  z-index: 1000;
  display: flex;
  align-items: center;
  justify-content: center;
}

.role-modal {
  width: 600rpx;
  background: white;
  border-radius: 20rpx;
  overflow: hidden;
}

.role-modal-header {
  padding: 40rpx 30rpx;
  text-align: center;
}

.role-modal-header.good {
  background: linear-gradient(135deg, #4caf50 0%, #388e3c 100%);
}

.role-modal-header.evil {
  background: linear-gradient(135deg, #f44336 0%, #c62828 100%);
}

.role-modal-title {
  display: block;
  font-size: 40rpx;
  font-weight: bold;
  color: white;
  margin-bottom: 10rpx;
}

.role-modal-side {
  display: block;
  font-size: 28rpx;
  color: rgba(255, 255, 255, 0.9);
}

.role-modal-content {
  padding: 30rpx;
}

.role-modal-name {
  font-size: 36rpx;
  font-weight: bold;
  color: #333;
  text-align: center;
  margin-bottom: 20rpx;
}

.role-modal-message {
  font-size: 30rpx;
  color: #333;
  line-height: 1.6;
  margin-bottom: 20rpx;
}

.role-modal-players {
  margin-top: 20rpx;
  padding: 20rpx;
  background: #f5f5f5;
  border-radius: 12rpx;
}

.role-modal-subtitle {
  font-size: 28rpx;
  font-weight: bold;
  color: #666;
  margin-bottom: 15rpx;
}

.role-modal-player {
  padding: 15rpx;
  background: white;
  border-radius: 8rpx;
  margin-bottom: 10rpx;
  font-size: 26rpx;
  color: #333;
}

.role-modal-player:last-child {
  margin-bottom: 0;
}

.player-num {
  color: #667eea;
  font-weight: bold;
  margin-right: 10rpx;
}

.player-nickname {
  margin-right: 10rpx;
}

.player-role {
  color: #999;
}

.role-modal-tip {
  margin-top: 20rpx;
  padding: 20rpx;
  background: #fff3cd;
  border-left: 4rpx solid #ffc107;
  border-radius: 8rpx;
  font-size: 26rpx;
  color: #856404;
  line-height: 1.5;
}

.role-modal-footer {
  padding: 20rpx 30rpx 30rpx;
}

.btn-close {
  width: 100%;
  height: 88rpx;
  line-height: 88rpx;
  background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
  color: white;
  border-radius: 44rpx;
  font-size: 32rpx;
  font-weight: bold;
  border: none;
}

/* 任务投票记录弹窗 */
.vote-modal {
  width: 650rpx;
  max-height: 80vh;
  background: white;
  border-radius: 24rpx;
  overflow: hidden;
  display: flex;
  flex-direction: column;
}

.vote-modal-header {
  padding: 30rpx;
  background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
  text-align: center;
  color: white;
}

.vote-modal-title {
  font-size: 34rpx;
  font-weight: bold;
}

.vote-modal-content {
  flex: 1;
  padding: 30rpx;
  overflow-y: auto;
}

.vote-round {
  margin-bottom: 20rpx;
  border-radius: 16rpx;
  border: 2rpx solid #f0f0f0;
}

.vote-round-header {
  padding: 20rpx;
  font-size: 30rpx;
  font-weight: bold;
  color: #333;
  background: #fafafa;
  border-bottom: 2rpx solid #f0f0f0;
}

.vote-round-list {
  padding: 10rpx 20rpx 20rpx;
  display: flex;
  flex-direction: column;
  gap: 12rpx;
}

.vote-round-player {
  display: flex;
  align-items: center;
  background: #fff;
  border-radius: 10rpx;
  padding: 15rpx 20rpx;
  box-shadow: 0 2rpx 6rpx rgba(0, 0, 0, 0.05);
}

.vote-player-number {
  font-weight: bold;
  color: #667eea;
  width: 100rpx;
}

.vote-player-name {
  flex: 1;
  color: #333;
}

.vote-player-result {
  font-weight: bold;
}

.vote-player-result.approve {
  color: #4caf50;
}

.vote-player-result.reject {
  color: #f44336;
}

.vote-round-empty {
  text-align: center;
  color: #666;
  padding: 60rpx 20rpx;
  font-size: 28rpx;
}

.vote-modal-footer {
  padding: 20rpx 30rpx 30rpx;
}


/* 任务结果显示 */
.mission-result-display {
  margin-bottom: 20rpx;
  padding: 20rpx;
  background: linear-gradient(135deg, #f5f5f5 0%, #fafafa 100%);
  border-radius: 16rpx;
  border: 2rpx solid #e0e0e0;
}

.mission-result-title {
  text-align: center;
  font-size: 32rpx;
  font-weight: bold;
  margin-bottom: 15rpx;
  padding: 10rpx;
  border-radius: 12rpx;
}

.mission-result-title.success {
  color: #4caf50;
  background: rgba(76, 175, 80, 0.1);
}

.mission-result-title.failed {
  color: #f44336;
  background: rgba(244, 67, 54, 0.1);
}

.mission-result-stats {
  display: flex;
  justify-content: space-around;
  gap: 20rpx;
}

.result-stat {
  flex: 1;
  display: flex;
  flex-direction: column;
  align-items: center;
  padding: 15rpx;
  background: white;
  border-radius: 12rpx;
  box-shadow: 0 2rpx 6rpx rgba(0, 0, 0, 0.05);
}

.result-stat.success {
  border: 2rpx solid #4caf50;
}

.result-stat.failed {
  border: 2rpx solid #f44336;
}

.stat-label {
  font-size: 24rpx;
  color: #666;
  margin-bottom: 8rpx;
}

.result-stat.success .stat-value {
  font-size: 36rpx;
  font-weight: bold;
  color: #4caf50;
}

.result-stat.failed .stat-value {
  font-size: 36rpx;
  font-weight: bold;
  color: #f44336;
}

.mission-participants {
  margin-top: 15rpx;
  padding: 12rpx 16rpx;
  background: white;
  border-radius: 12rpx;
  text-align: center;
  font-size: 24rpx;
}

.participants-label {
  color: #999;
  margin-right: 8rpx;
}

.participants-numbers {
  color: #667eea;
  font-weight: bold;
}

.vote-history-divider {
  margin: 20rpx 0;
  text-align: center;
  position: relative;
}

.vote-history-divider::before,
.vote-history-divider::after {
  content: '';
  position: absolute;
  top: 50%;
  width: 35%;
  height: 2rpx;
  background: #e0e0e0;
}

.vote-history-divider::before {
  left: 0;
}

.vote-history-divider::after {
  right: 0;
}

.vote-history-title {
  display: inline-block;
  padding: 0 20rpx;
  font-size: 26rpx;
  color: #999;
  background: white;
}

/* 任务结果弹窗 */
.mission-result-modal {
  width: 90%;
  max-width: 600rpx;
  animation: modalSlideIn 0.3s ease-out;
}

@keyframes modalSlideIn {
  from {
    opacity: 0;
    transform: translateY(-50rpx) scale(0.9);
  }
  to {
    opacity: 1;
    transform: translateY(0) scale(1);
  }
}

.mission-result-modal-content {
  background: white;
  border-radius: 32rpx;
  padding: 60rpx 40rpx 40rpx;
  text-align: center;
  position: relative;
  overflow: hidden;
}

.mission-result-modal-content::before {
  content: '';
  position: absolute;
  top: 0;
  left: 0;
  right: 0;
  height: 200rpx;
  opacity: 0.1;
}

.mission-result-modal-content.success::before {
  background: linear-gradient(135deg, #4caf50 0%, #66bb6a 100%);
}

.mission-result-modal-content.failed::before {
  background: linear-gradient(135deg, #f44336 0%, #ef5350 100%);
}

.result-icon-wrapper {
  margin-bottom: 30rpx;
  animation: iconBounce 0.6s ease-out;
}

@keyframes iconBounce {
  0% {
    transform: scale(0);
  }
  50% {
    transform: scale(1.2);
  }
  100% {
    transform: scale(1);
  }
}

.result-icon {
  width: 140rpx;
  height: 140rpx;
  border-radius: 50%;
  display: flex;
  align-items: center;
  justify-content: center;
  font-size: 80rpx;
  font-weight: bold;
  color: white;
  margin: 0 auto;
  box-shadow: 0 8rpx 24rpx rgba(0, 0, 0, 0.15);
}

.result-icon.success {
  background: linear-gradient(135deg, #4caf50 0%, #66bb6a 100%);
}

.result-icon.failed {
  background: linear-gradient(135deg, #f44336 0%, #ef5350 100%);
}

.result-title {
  font-size: 48rpx;
  font-weight: bold;
  margin-bottom: 20rpx;
  background: linear-gradient(135deg, #333 0%, #666 100%);
  -webkit-background-clip: text;
  -webkit-text-fill-color: transparent;
  background-clip: text;
}

.result-mission-number {
  font-size: 28rpx;
  color: #999;
  margin-bottom: 40rpx;
}

.result-cards {
  display: flex;
  gap: 20rpx;
  margin-bottom: 40rpx;
  animation: cardsFadeIn 0.4s ease-out 0.2s both;
}

@keyframes cardsFadeIn {
  from {
    opacity: 0;
    transform: translateY(20rpx);
  }
  to {
    opacity: 1;
    transform: translateY(0);
  }
}

.result-card {
  flex: 1;
  background: linear-gradient(135deg, #f5f5f5 0%, #fafafa 100%);
  border-radius: 24rpx;
  padding: 30rpx 20rpx;
  box-shadow: 0 4rpx 12rpx rgba(0, 0, 0, 0.08);
  position: relative;
  overflow: hidden;
}

.result-card::before {
  content: '';
  position: absolute;
  top: 0;
  left: 0;
  right: 0;
  height: 6rpx;
}

.result-card.success-card::before {
  background: linear-gradient(90deg, #4caf50 0%, #66bb6a 100%);
}

.result-card.fail-card::before {
  background: linear-gradient(90deg, #f44336 0%, #ef5350 100%);
}

.card-icon {
  font-size: 40rpx;
  font-weight: bold;
  margin-bottom: 15rpx;
}

.success-card .card-icon {
  color: #4caf50;
}

.fail-card .card-icon {
  color: #f44336;
}

.card-label {
  font-size: 24rpx;
  color: #999;
  margin-bottom: 10rpx;
}

.card-value {
  font-size: 56rpx;
  font-weight: bold;
  line-height: 1;
  margin-bottom: 5rpx;
}

.success-card .card-value {
  color: #4caf50;
}

.fail-card .card-value {
  color: #f44336;
}

.card-unit {
  font-size: 22rpx;
  color: #999;
}

.result-btn-wrapper {
  animation: btnFadeIn 0.4s ease-out 0.3s both;
}

@keyframes btnFadeIn {
  from {
    opacity: 0;
  }
  to {
    opacity: 1;
  }
}

.result-btn {
  width: 100%;
  height: 88rpx;
  line-height: 88rpx;
  background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
  color: white;
  border-radius: 44rpx;
  font-size: 32rpx;
  font-weight: bold;
  border: none;
  box-shadow: 0 6rpx 20rpx rgba(102, 126, 234, 0.4);
  transition: all 0.3s;
}

.result-btn:active {
  transform: scale(0.98);
  box-shadow: 0 4rpx 12rpx rgba(102, 126, 234, 0.3);
}
