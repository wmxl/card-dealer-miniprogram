<!--pages/create/create.wxml-->
<view class="container">
  <view class="header">
    <text class="title">创建房间</text>
    <text class="subtitle">选择房间人数（5-10人）</text>
  </view>

  <view class="content">
    <view class="player-selector">
      <view
        class="player-option {{maxPlayers === item ? 'active' : ''}}"
        wx:for="{{[5,6,7,8,9,10]}}"
        wx:key="*this"
        data-value="{{item}}"
        bindtap="selectPlayers"
      >
        <text>{{item}}人</text>
      </view>
    </view>

    <button
      class="btn btn-primary"
      bindtap="createRoom"
      loading="{{loading}}"
      disabled="{{loading}}"
    >
      创建房间
    </button>
  </view>
</view>
